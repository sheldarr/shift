let chai = require('chai');
let chaiHttp = require('chai-http');

chai.use(chaiHttp);

describe('Resources', () => {
    beforeEach(() => {
    });

    describe('/GET', () => {
        it('Should pass', () => {
        });
    });
});