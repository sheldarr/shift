'use strict';

import React from 'react';

const Dashboard = React.createClass({
    render () {
        return (
            <div>
                {'Dashboard'}
            </div>
		);
    }
});

export default Dashboard;
